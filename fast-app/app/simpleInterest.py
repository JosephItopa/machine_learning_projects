def simple_interest(principal, rate, period):
    """
    A simple interest function to evaluate the amount of interest
    from a client's principal.
    """
    interest= (principal * rate * period)/100

    return interest